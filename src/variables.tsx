type RoutesType = {
  forside: string;
  main: string;
  gameSetup: string;
  game: string;
};

export const routes: RoutesType = {
  forside: "/",
  main: "/main",
  gameSetup: "/gameSetup",
  game: "/game",
};
